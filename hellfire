local util = hell.utils

hell.outdir = 'build'

copy {
	input = util.glob 'src/lua/*.lua'
}
copy {
	input = 'src/builders'
}
build {
	input = util.glob 'src/haskell/*.hs',
	flags = '&-dynamic',
	output = 'hell'
}
