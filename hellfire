local util = hell.utils

hell.outdir = 'build'

copy {
	input = util.glob 'src/lua/*.lua'
}
copy {
	input = 'src/builders'
}
cpp.shared {
	input = util.glob 'src/cpp/*.cpp',
	flags = '&-Wall -g -std=c++11',
	links = 'lua',
	output = 'cppUtils.so'
}
